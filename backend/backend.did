type issue = record {
    state: opt text;
    login: opt text;
    id: opt text;
    milestone_state: opt text;
    closed_at: opt text;
};

type gh_token = text;

service : {
    "get_gh_issue": (gh_token) -> (issue);
}